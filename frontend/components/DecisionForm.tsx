'use client';
import {useState} from 'react';
export function DecisionForm({id}:{id:string}){const [actor,setActor]=useState(''),[reason,setReason]=useState(''),[message,setMessage]=useState('');async function decide(action:string){if(!actor.trim()||!reason.trim()){setMessage('Actor and reason are required');return}const r=await fetch(`/api/releases/${id}/${action}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({actor,reason})});setMessage(r.ok?`${action} completed`:'Decision failed');if(r.ok)location.reload()}return <section><h2>Human decision</h2><label>Actor<input value={actor} onChange={e=>setActor(e.target.value)}/></label><label>Reason<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><div className="actions"><button onClick={()=>decide('approve')}>Approve and publish</button><button className="danger" onClick={()=>decide('reject')}>Reject</button></div><p role="alert">{message}</p></section>}
