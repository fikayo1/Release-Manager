'use client';
import {useState} from 'react';
export function ScheduleForm({schedule}:{schedule:any}){const [expression,setExpression]=useState(schedule.expression),[enabled,setEnabled]=useState(schedule.enabled),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMessage('Saving…');const r=await fetch('/api/schedule',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({expression,enabled})});const d=await r.json();setMessage(r.ok?'Schedule saved':d.detail||'Could not save')}return <form onSubmit={submit}><label>Cron expression (UTC)<input value={expression} onChange={e=>setExpression(e.target.value)} required/></label><label className="check"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> Enabled</label><button>Save schedule</button><p role="status">{message}</p></form>}
