# Server-only URL for the separately deployed FastAPI Vercel project.
RELEASE_MANAGER_API_URL=https://your-release-manager-api.vercel.app

# Used only by the Vercel Cron proxy route. Keep it identical to the backend.
CRON_SECRET=generate-a-long-random-string
